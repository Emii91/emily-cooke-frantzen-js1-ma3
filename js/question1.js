// Convert the function below to an arrow function:

// function getRemainder(a,b) {
//     return a % b;
//     }

const getRemainder = (a, b) => a % b;

const result = getRemainder(2, 10);

console.log(result);